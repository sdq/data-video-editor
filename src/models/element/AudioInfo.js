export default class AudioInfo {
    constructor(name, src, duration, background, volume) {
        this.name = name;
        this.src = src;
        this.duration = duration;
        this.background = background;
        this.volume = volume;
    }
}
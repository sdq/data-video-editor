//import { createSelector } from 'reselect';

export const isPerforming = state => state.player.isPerforming;
export const isVideoPerforming = state => state.player.isVideoPerforming;
export const isScenePerforming = state => state.player.isScenePerforming;